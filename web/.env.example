# Base URL of the nutrition-api service. Local dev points at the uvicorn
# server on :8000; production points at the deployed API origin (see
# render.yaml). Leave empty only if the dashboard and API share an origin.
VITE_API_BASE=http://localhost:8000
